{
  "name": "functions",
  "description": "Cloud Functions for Firebase",
  "version": "0.0.1",
  "dependencies": {
    "express": "^4.16.4",
    "firebase-admin": "~7.0.0",
    "firebase-functions": "^2.2.0",
    "secure-compare": "^3.0.1",
    "twitter": "^1.7.1"
  },
  "devDependencies": {
    "eslint": "^5.12.0",
    "eslint-config-prettier": "^4.0.0",
    "eslint-plugin-prettier": "^3.0.1",
    "prettier": "^1.16.4"
  },
  "engines": {
    "node": ">=8"
  },
  "private": true,
  "scripts": {
    "f:deploy": "firebase deploy --only functions",
    "f:lint": "eslint -f codeframe --ext .js --ignore-path .gitignore .",
    "f:logs": "firebase functions:log",
    "f:serve": "firebase serve --only functions",
    "f:shell": "firebase functions:shell",
    "f:start": "npm run shell",
    "deploy": "gcloud app deploy",
    "start": "node app.js",
    "system-test": "repo-tools test app",
    "test": "npm run system-test",
    "e2e-test": "repo-tools test deploy"
  }
}
